{
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon --watch . ./app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose-unique-validator": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
